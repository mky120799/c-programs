#include<iostream>
using namespace std;
#include<stdlib.h>
#include<stdio.h>
#include<conio.h>

class book {
    char title;
    
  // body of the class
};
 void book :: getdata(char a[],float b) { 
    // defining member function outside the class
    strcpy(title,a):
    price = b:
}
  void book :: putdata () {
    cout<<"\nTitle of Book: "<<title;
    cout<<"\nPrice of Book: "<<price;
}